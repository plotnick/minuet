workspace = { members = ["solver", "syntax", "tracer"] }

[package]
name = "minuet"
version = "0.0.1"
edition = "2021"

[dependencies]
gray-codes = "0.1"
minuet-solver = { path = "solver" }
minuet-syntax = { path = "syntax" }
minuet-tracer = { path = "tracer" }

[dev-dependencies]
