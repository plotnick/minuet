[package]
name = "minuet-semantics"
version = "0.0.1"
edition = "2021"

[lib]
path = "semantics.rs"

[dependencies]
gray-codes.workspace = true
minuet-solver = { path = "../solver" }
minuet-syntax = { path = "../syntax" }
minuet-tracer = { path = "../tracer" }
